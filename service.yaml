apiVersion: v1
kind: Service
metadata:
  name: f21ao
  labels:
    app.kubernetes.io/name: f21ao-service
    app.kubernetes.io/component: f21aoservice
spec:
  type: NodePort
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/name: f21ao-service
    app.kubernetes.io/component: f21aoservice
